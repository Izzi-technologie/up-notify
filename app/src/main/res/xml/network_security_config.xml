<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext is allowed so a local http API can be used during setup. Prefer https and wss. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
